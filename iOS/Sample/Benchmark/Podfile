# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Benchmark' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Logan', '~> 1.2.5'
  # Pods for Benchmark

  target 'BenchmarkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BenchmarkUITests' do
    # Pods for testing
  end

end
